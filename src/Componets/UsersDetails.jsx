import { useLoaderData } from "react-router-dom";


const UsersDetails = () => {
    const user=useLoaderData()
    // console.log(user);
    return (
        <div>
            
        </div>
    );
};

export default UsersDetails;